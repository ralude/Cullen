# Sub-fase 11.03: JWT y sesiones

- **Fase:** [Fase 11 - Seguridad](./README.md)
- **Estado:** Completada el 2026-09-08. La prueba del arranque empaquetado, que se cerró
  declarando la brecha, la cubrió después el
  [paquete pre-piloto](../pre-piloto/plan-empaquetado-nodo.md) el 2026-09-09

## Objetivo

Proteger sesiones y transporte HTTP sin exponer secretos al renderer.

## Política aprobada

Se eligen sesiones opacas revocables, no JWT. El token aleatorio de 256 bits se
guarda solo como hash SHA-256 y viaja en cookie `HttpOnly`, `SameSite=Strict`.
Expira a los 30 minutos de inactividad o a las 8 horas absolutas. Terminal y
nodo proceden del archivo local protegido definido por ADR-0011.

## Tareas

### Deuda documentada de la auditoría 2026-09-04

- [x] Mantener el servidor en loopback mientras no exista una composición LAN
  segura; rechazar una configuración de `SERVER_HOST` que exponga operaciones
  por accidente.
- [x] Definir y probar la política de cookie para cada transporte permitido,
  incluyendo `Secure` cuando corresponda.
- [x] Probar el arranque empaquetado con el servidor local, una sesión autenticada, una
  operación y recuperación tras reinicio sin proxy de Vite. El empaquetado pertenece a su fase
  propietaria; este punto verifica la frontera de sesión y transporte. Cerrado el 2026-09-09 por
  el [paquete pre-piloto](../pre-piloto/plan-empaquetado-nodo.md): el artefacto que se prueba es
  el bundle del servicio de Windows de
  [ADR-0030](../../architecture/adr/0030-empaquetado-y-runtime-del-nodo.md), no un proceso hijo
  de Electron. El MSI sin firmar y su validación en tienda siguen abiertos en el gate.
- [x] Mantener pruebas de suplantación de nodo/terminal y de configuración
  insegura antes de habilitar LAN.

- [x] ~~Elegir política de sesiones opacas y expiración.~~
- [x] ~~Implementar sesiones revocables para el despliegue local.~~
- [x] ~~Validar revocación y renovación de expiración idle.~~
- [x] ~~Probar expiración, cambio de autorización y permisos insuficientes.~~

## Criterio de salida

Las peticiones HTTP tienen una identidad valida y una sesion controlable.

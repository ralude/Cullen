# Fase 12.05 — Mantenibilidad estructural y eficiencia de contexto

- **Fase:** [12 — Optimización](./README.md).
- **Estado:** Planificada el 2026-09-08; diagnóstico y baseline estática disponibles,
  refactors y benchmark posterior pendientes.
- **Gate:** cerrar Fase 11 y habilitar Fase 12 según el [cronograma](../README.md).
  Esta planificación no adelanta implementación, no reabre Fase 10 ni cambia el gate fiscal.
- **Evidencia inicial:** [diagnóstico](./diagnostico-12.05.md) y
  [manifiesto reproducible](./context-radius-baseline.json), sobre `a73dd33`.

## Objetivo y límites

Reducir el conjunto de archivos y conocimiento ajeno necesario para modificar una feature:
dominio, aplicación, puertos, adaptadores pertinentes y UI. Optimizar el radio de contexto,
no LOC, cantidad de archivos o apariencia de modularidad. Una extracción debe reducir
superficie no relacionada sin aumentar significativamente los saltos de navegación.

Se conserva composición TypeScript explícita, exports públicos, comportamiento observable,
contratos HTTP/eventos, ownership y límites transaccionales. No se introducen contenedores DI,
service locators, reflection, registro mágico, repositorios universales, clases base genéricas,
interfaces sin consumidor ni wrappers de una línea sin frontera semántica. No se crean
microarchivos, barrels globales nuevos ni cajones `utils`/`shared`. La pequeña duplicación no
justifica una abstracción. No se agregan dependencias para este trabajo.

Gobiernan [capas](../../architecture/01-capas.md),
[módulos](../../architecture/02-modulos.md), ADR-0001, ADR-0002, ADR-0003 y ADR-0009.
Para composición de venta y LAN se añaden ADR-0026 y el escenario
[FS-005](../../failure-scenarios/FS-005-venta-concurrente-ultima-unidad.md).
Una reubicación que conserva estas decisiones no necesita un ADR nuevo. Si exige cambiar
autoridad, atomicidad, fallos o contratos, se detiene ese corte y se decide normativamente
antes de implementarlo; no se modifica una especificación para hacer pasar tests.

## 12.05.01 — Context Radius Baseline

- [x] Inspeccionar hubs, consumidores, imports, tests e historia reciente.
- [x] Registrar seis escenarios concretos y listas de archivos verificables.
- [ ] Al habilitar ejecución, refrescar baseline contra el HEAD vigente sin borrar este corte.
- [ ] Registrar sesiones reales de navegación de los mismos escenarios antes de refactorizar.

El manifiesto es una shortlist estática estimada, no una medición de sesiones humanas o IA.
Separar archivos de producción, tests, instrucciones/documentación y archivos editados.
Contar cada ruta una vez por escenario. Los contextos de negocio se cuentan aparte de las
capas técnicas; identidad, moneda y primitivas transitivas se explican cuando deban leerse.
Registrar consultas de búsqueda, secciones efectivamente leídas, profundidad de navegación
y causa de cada archivo adicional. No contar un import como una lectura íntegra obligatoria.

Aceptación: cualquier persona puede reconstruir las listas, sus conteos y superficie de
búsqueda desde la revisión fijada. No se prometen ahorros de tokens sin telemetría comparable.

## 12.05.02 — Server composition locality

- [ ] Extraer composición cohesiva donde la baseline muestre retorno, empezando por un módulo
  de bajo acoplamiento (proveedores o reportes); comparar antes de extender el patrón.
- [ ] Mantener un ensamblador explícito y parámetros concretos; no pasar todo el runtime a
  cada módulo. Las piezas comunes conservan una sola instancia por nodo.
- [ ] Mantener `createSecurityRuntime`, `SecurityRuntime` y `ADMIN_PERMISSIONS` para los
  consumidores actuales; evitar renombrado masivo de entrada.
- [ ] Revisar también `app.ts`: `ServerDependencies` y helpers HTTP son consumidos por rutas.
  Separar el contrato/helpers del registrador solo si evita cargar el hub al editar una ruta;
  no hacer que los módulos importen la implementación del runtime para obtener sus tipos.

Aceptación: una dependencia local se conecta en su composición local; el root declara las
relaciones entre módulos. Se preservan el emisor fiscal compartido, consentimiento X/Z,
`ambientUnitOfWork`, cobro local atómico, relay de inventario standalone y consumo LAN.
No se instancian dos DB, impresoras o workers. Verificar contratos HTTP, venta real y E2E LAN.

## 12.05.03 — Persistence locality

- [ ] Separar las nueve clases de `repositories.ts` por contexto real: catálogo (producto,
  categoría, unidad), moneda (tasa, método de pago), caja (caja y turno), ventas e inventario.
  Un archivo cohesivo por grupo es suficiente; carpetas solo cuando aporten navegación.
- [ ] Seguir el patrón existente de `supplier-repository.ts` y conservar exports de DB.
- [ ] Reorientar imports internos de productores, snapshots, lecturas y tests al dueño concreto.
- [ ] Resolver el helper `read` sin repository base ni capa adicional; reutilizar la traducción
  de errores de unidad de trabajo si corresponde y comparar el costo de una pequeña duplicación.

Aceptación: cambios de mapeo de inventario no requieren las clases de caja/ventas; consultas,
rehidratación, orden de movimientos y restricciones siguen idénticos. No cambia schema ni
migraciones por mover implementaciones. Pruebas DB, ledger, caja, referencias y sync pertinentes.

## 12.05.04 — Desktop API locality

- [ ] Separar transporte (`requestJson`, error HTTP, cookies, serialización) de operaciones por
  feature. Reutilizar contratos compartidos; no duplicar rutas o permisos.
- [ ] Probar un grupo semántico y su consumidor antes de extender `api.sales`, `api.cash`, etc.
- [ ] Acotar el tipo recibido por cada pantalla al grupo que usa; revisar `ScreenProps` y mocks.
  Evitar una fachada plana permanente que duplique la API nueva.
- [ ] Ubicar conversión de entradas monetarias/escaladas en una responsabilidad de presentación
  reconocible: no obligar a cargar todo el transporte para formatear una cantidad.

Aceptación: añadir una operación local no modifica clientes ajenos. Preservar fetch inyectable,
`credentials: include`, 204, errores, encoding de rutas/query, idempotency keys y payloads.
Validar cliente HTTP, interacción de consumidores y E2E de venta. Migrar por cortes coherentes,
sin romper mocks parciales ni convertir cambios de tipos en modificaciones cosméticas masivas.

## 12.05.05 — Agent instruction locality

- [x] Reconocer la jerarquía existente y el commit `c39f43c`; no recrearla.
- [x] Verificar que root contiene reglas universales y locales separan core/DB/server/desktop.
- [ ] Actualizar rutas fuente de verdad en el mismo commit que mueva su responsabilidad.
- [ ] Añadir instrucciones más profundas solo ante una tarea que hoy cargue reglas ajenas.

Aceptación: root sigue siendo el índice universal; instrucciones de Electron no se añaden a
core y las de Drizzle no se añaden a apps. Sin duplicación normativa ni commit artificial
para presentar como nuevo lo ya existente. En cada tarea leer root más locales aplicables.

## 12.05.06 — UI/application hotspots

- [ ] Evaluar navegación pura de `App.tsx` como corte independiente de sesión, shell y errores;
  conservar componentes cohesionados y rutas hash, sin agregar router.
- [ ] Evaluar recepción de compras dentro de `screens/inventory.tsx`: tiene documento,
  proveedor, costo y ciclo propios frente al kardex y ajustes. Mantener feedback y refrescos.
- [ ] Revisar reportes solo si la tarea de un campo evidencia navegación sobrante: las secciones
  semánticas ya existen como componentes locales.
- [x] Descartar por ahora división de schema, pantallas de caja/venta y protocolo/worker sync
  solo por tamaño o transversalidad; razones en el diagnóstico.

Aceptación: interacción observable de permisos, recuperación de sesión, atajos, errores,
selección de producto y envío de comandos permanece igual. Una extracción debe permitir
trabajar en una responsabilidad sin leer sus hermanas. No se impone límite de líneas.

## 12.05.07 — Architectural boundary protection

- [ ] Extender ESLint y `tests/eslint-boundaries.test.ts`, sin herramientas adicionales.
- [ ] Añadir fixtures negativos y positivos: renderer → DB/core/Node; domain → application,
  apps/drivers; application → adaptadores; imports internos entre paquetes.
- [ ] Proteger nuevos módulos de imports de implementación hacia el composition root.
- [ ] Cubrir formas de importación relativas, aliases públicos y reexports relevantes; las
  reglas de patrones actuales no prueban por sí solas todos esos caminos.
- [ ] Para cruces de contexto, proteger el puerto concreto acordado, sin prohibir los cruces
  legítimos sales → cash/inventory ni inventar una independencia absoluta.

Aceptación: cada frontera nueva falla con una violación representativa y acepta su uso válido.
Las pruebas existentes de renderer son seis probes; no se presentan como análisis completo
del grafo. Incorporar protección junto al corte que introduce la frontera, no solo al final.

## 12.05.08 — AI maintainability benchmark

- [ ] Repetir exactamente los seis escenarios sobre el commit final, con el mismo procedimiento.
- [ ] Publicar por escenario BEFORE/AFTER: listas y conteos de archivos de producción y tests,
  contextos, edición real/estimada, composición, superficie relevante y profundidad de navegación.
- [ ] Separar medición observada, estimación y expectativa; documentar casos que empeoren.
- [ ] Comprobar descubribilidad desde el nombre de la feature, sin depender del historial del chat.

Aceptación: reducción verificable de exposición a código ajeno en los escenarios afectados,
sin trasladar el costo a un nuevo hub ni aumentar saltos sin justificación. No exigir una
reducción artificial de archivos a tareas intrínsecamente transversales. Descartar una extracción
que no demuestre beneficio. Este corte no tiene AFTER ni certifica ahorro.

## Orden, validación y commits

Primero baseline/documentación y reconocimiento de instrucciones existentes. Tras el gate,
priorizar persistencia (menor riesgo observado), composición, cliente HTTP y cortes UI justificados;
acompañar cada frontera con tests. Terminar con benchmark. La numeración no obliga a extraer
runtime antes de DB y 12.04 suspendida no impide 12.05 una vez habilitada Fase 12.

Hitos previstos, cada uno condicionado a evidencia y checks verdes:

1. `docs(architecture): plan context radius optimization` — diagnóstico, baseline y cronograma.
2. `refactor(db): localize repository implementations` — clases base, exports y consumidores.
3. `refactor(server): localize runtime composition` — separar primero un grupo verificable;
   repetir por capacidad si el resto exige cortes independientes.
4. `refactor(desktop): group API operations by feature` — transporte y grupos con sus consumidores.
5. `refactor(desktop): isolate navigation from application shell` — solo si el ensayo lo justifica.
6. `refactor(desktop): isolate purchase receipt workflow` — solo con interacción preservada.
7. `test(architecture): protect module composition boundaries` — únicamente cobertura transversal
   restante; las fronteras de cada extracción se validan en su propio hito.
8. `docs(architecture): record context radius comparison` — evidencia final, no ahorro supuesto.

Antes de cada commit: status, diff, lint, typecheck, tests pertinentes y boundaries; stage con
rutas explícitas y revisión de `git diff --cached`. No commitear fallos conocidos ni trabajo ajeno.
Para código ejecutar además el mínimo global `pnpm lint`, `pnpm typecheck`, `pnpm test`;
infraestructura transversal exige suites de integración/E2E relevantes. Para documentación,
lint, typecheck solicitado y pruebas arquitectónicas existentes, rutas y `git diff --check`;
no ejecutar suites funcionales costosas sin cambio funcional. Registrar resultados al cerrar
el hito. No modificar semántica de negocio dentro de un refactor estructural.

## Validación del hito documental — 2026-09-08

- `pnpm lint`: aprobado.
- `pnpm typecheck`: aprobado en los diez paquetes seleccionados.
- `pnpm test tests/eslint-boundaries.test.ts`: seis pruebas aprobadas.
- Enlaces Markdown locales y rutas del manifiesto: comprobados; `git diff --check`: aprobado.
- Typecheck y Vitest requirieron ejecución fuera del sandbox: dentro de él no se resolvieron
  declaraciones de `@fastify/static` y esbuild recibió acceso denegado al cargar configuración.
  Ambos checks pasaron al repetirlos sin esa restricción; no se modificaron dependencias.
- No se ejecutó la suite funcional completa: el hito solo cambia documentación y datos de
  baseline. No hay refactor, cambio de contratos ni benchmark AFTER que certificar.

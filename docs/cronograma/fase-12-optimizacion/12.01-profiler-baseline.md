# Sub-fase 12.01: Baseline y presupuestos de rendimiento

- **Fase:** [Fase 12 — Optimización](./README.md).
- **Estado:** En ejecución desde el 2026-09-11. Protocolo fijado, piloto de ruido corrido y
  perfiles de datos construidos; el de crecimiento ya midió el riesgo de rehidratación que la
  Fase 11 dejó anotado. La jornada del escenario 4 quedó instrumentada entera —apertura de caja
  y emisión simulada incluidas— y con ella se registró el defecto D-003. El escenario 2 ya corre
  sobre el renderer compilado en Electron, Fastify por loopback y recuperación de la cookie de
  sesión. Falta instrumentar el ciclo LAN, completar la separación del arranque del nodo y las
  métricas de recursos, y después correr la serie BEFORE, fijar presupuestos y priorizar.
- **Entrada:** V0.1.04 cerrada y commit candidato de medición sin cambios pendientes.
- **Salida habilitada:** [12.02 — Comunicación entre procesos](./12.02-ipc.md).
- **Plan de ejecución:** [cortes, validación y decisión de uso pendiente](./plan-12.01-baseline.md).

## Objetivo

Construir una línea base reproducible de CPU, memoria, latencia, throughput y crecimiento de
datos antes de modificar implementaciones. Esta sub-fase fija cómo medir y cuáles resultados
necesitan intervención; no presupone que exista un problema ni exige optimizar para producir
un diff.

## Fuentes y límites

Gobiernan el [alcance del MVP](../../producto/alcance-entregas.md), la
[arquitectura](../../architecture/README.md), ADR-0007 y los contratos ya entregados. Cada
escenario usa `FiscalPrinterFake` y conserva visible `SIMULACION`. No se conecta hardware
fiscal, no se alteran reglas de negocio y no se introducen dependencias de profiling mientras
las APIs de Node.js, Chromium, Electron, SQLite o el sistema operativo sean suficientes.

Los umbrales no se eligen por intuición. Primero se ejecuta un piloto de medición, se cuantifica
la variabilidad y luego se registran presupuesto, muestra, warm-up y tolerancia de regresión
antes de tocar el código. Si el comportamiento ya cumple el presupuesto acordado, la evidencia
de «sin cambio» es una salida válida.

## Escenarios obligatorios

La misma semilla, comandos y equipo se conservan para BEFORE y AFTER:

1. Arranque frío hasta que servidor y desktop estén listos para ingresar.
2. Ingreso, recuperación de sesión y carga inicial del shell autorizado.
3. Consulta de catálogo por listado y barcode sobre el conjunto de datos representativo.
4. Jornada principal: abrir caja, crear y completar una venta y emitir el documento simulado.
5. Consulta de kardex y reportes con historia de inventario en varios tamaños controlados.
6. Ciclo LAN reproducible: reclamar, entregar y aplicar un lote entre terminal y coordinador,
   incluida una reconexión, sin usar una red externa.

Los tamaños exactos de catálogo, ventas, movimientos y eventos se fijan en el manifiesto de
medición después del piloto. Deben incluir un caso habitual y otro que haga visible el
crecimiento; no se cambia el dataset entre revisiones para favorecer el resultado.

## Métricas y protocolo

- Identificar commit, Windows, CPU, RAM, almacenamiento, modo de energía, Node.js, pnpm,
  Electron y configuración funcional no secreta.
- Separar arranque frío de ejecución caliente y registrar warm-up, repeticiones y descartes.
- Medir tiempo extremo a extremo y por proceso; CPU, working set/RSS y pico de memoria;
  cantidad/tamaño de intercambios; consultas, tiempo SQLite y tamaño de la base cuando aplique.
- Reportar observaciones individuales y resumen con mediana, percentiles útiles, mínimo y
  máximo. No presentar un percentil que el tamaño de muestra no sostenga.
- Ejecutar BEFORE y AFTER en el mismo entorno. Si cambia hardware, runtime, datos o protocolo,
  se crea otra serie y no se mezclan resultados.
- Registrar ruido conocido, variación entre corridas y margen mínimo que debe superar un cambio
  para considerarse mejora en lugar de fluctuación.
- Mantener trazas y datos voluminosos fuera de Git; versionar el manifiesto, el resumen y los
  comandos suficientes para reproducirlos, sin PIN, tokens, cookies, claves o payloads sensibles.

## Tareas

- [x] **12.01.01 — Manifiesto:** definir formato de entorno, dataset, escenario, comando,
  warm-up, muestra y artefactos; decidir una ubicación ignorada para las trazas crudas.
  Entregado en [`performance-manifest.json`](./performance-manifest.json); las trazas crudas
  viven en `.perf/`, ignorado.
- [ ] **12.01.02 — Datos deterministas (parcial):** construir o reutilizar una semilla repetible para los
  seis escenarios, con reloj/IDs controlados donde la comparación lo requiera. Hecha la
  semilla del uso habitual —200 productos, caja, métodos y política de IGTF, reconstruida por
  corrida— y el de crecimiento, con historias de inventario de 100, 1.000 y 10.000
  movimientos construidas por el dominio. Las profundidades salen del riesgo que la auditoría
  de Fase 11 dejó sin benchmark, no de una tienda inventada: ver
  [perfil de crecimiento](./perfil-de-crecimiento.md). Siguen pendientes los datos del ciclo
  LAN y la carga operativa representativa; se corrige la marca de cierre
  parcial el 2026-09-12 sin retirar la evidencia entregada.
- [ ] **12.01.03 — Harness mínimo (parcial):** automatizar la preparación y captura con herramientas ya
  disponibles; una corrida fallida no se registra como una observación rápida. Entregado en
  `apps/server/perf/measure.ts`, sin dependencias nuevas: aborta la serie si un paso no
  responde como debe. El primer corte del 2026-09-12 verifica argumentos, aislamiento,
  cantidades, período y procedencia; el segundo cerró la jornada del escenario 4 —apertura de
  caja, emisión simulada y verificación de sus asientos—; el tercero ejecuta el build actual en
  Electron, mide llegada al ingreso, autorización del shell y recuperación de sesión por HTTP
  real. Faltan ciclo LAN, la separación completa del arranque del nodo y las métricas de recursos.
- [ ] **12.01.04 — Piloto de ruido (repetición pendiente):** repetir los escenarios, cuantificar variación y fijar el
  protocolo definitivo antes de declarar presupuestos. Corrido el 2026-09-11 sobre `163548c`:
  ver [piloto de ruido](./piloto-de-ruido.md). Cambió una decisión del manifiesto —el margen
  es la deriva de la mediana entre series, no el IQR dentro de una— y fijó el margen por
  escenario. Esa evidencia se conserva; los cambios del protocolo 2 requieren otro piloto
  y márgenes propios antes del BEFORE.
- [ ] **12.01.05 — Baseline BEFORE:** capturar resultados del commit limpio y conservar tanto
  valores como incidencias observadas.
- [ ] **12.01.06 — Presupuestos:** acordar por escenario límites de aceptación y regresión a
  partir de la línea base y de una expectativa de uso documentada.
- [ ] **12.01.07 — Priorización:** ordenar únicamente los hotspots cuya contribución sea medible
  y asignarlos a 12.02, 12.03 o 12.05; registrar explícitamente lo que no amerita cambios.

## Criterios de aceptación

- [ ] CA-12.01-01: otra persona reproduce la serie desde un checkout limpio siguiendo comandos
  documentados y obtiene resultados dentro del margen de variación registrado.
- [ ] CA-12.01-02: cada resultado identifica entorno, commit, dataset, muestra y escenario; no
  combina arranque frío, warm-up y ejecución caliente.
- [ ] CA-12.01-03: los seis escenarios completan el mismo comportamiento funcional que antes y
  `SIMULACION` permanece explícita.
- [ ] CA-12.01-04: cada presupuesto tiene una fuente observable; no existe un porcentaje global
  inventado ni un promedio que oculte la regresión de un escenario.
- [ ] CA-12.01-05: la evidencia no contiene secretos ni datos comerciales reales y permite
  distinguir medición observada, estimación y expectativa.
- [ ] CA-12.01-06: no se inicia 12.02, 12.03 ni un refactor de 12.05 sin una comparación BEFORE
  aplicable al corte.

## Entregables y commits previstos

- Protocolo, manifiesto y semillas reproducibles.
- Resumen BEFORE con presupuestos y hotspots priorizados.
- Scripts de medición pequeños, si son necesarios, separados del código productivo.

Checkpoint sugerido: `perf(baseline): record phase 12 performance budgets`. Si el hito solo
agrega documentación y datos de medición, usar `docs(perf)` en lugar de afirmar un cambio de
rendimiento.

## Fuera de alcance

- Optimizar código, cambiar contratos, agregar índices o introducir cachés.
- Certificar capacidad de producción con una sola máquina de desarrollo.
- Usar duración de tests unitarios como sustituto de latencia operativa.
- Convertir el benchmark en una prueba frágil que falle por ruido del runner compartido.

## Corte de integridad del arnés — 2026-09-12

Primer corte del [plan](./plan-12.01-baseline.md) completado. Integra los reportes locales y
su siembra de 300 ventas, valida argumentos y cantidades, ancla el período comercial al
inicio del proceso, separa escenarios en procesos secuenciales y registra revisión completa,
cambios pendientes, hash del arnés y protocolo. El cobro de la semilla usa `TaxRate.includeIn`.
Se omite p90 con menos de 30 observaciones. Un escenario sin datos o mal declarado aborta.

La prueba inicial reprodujo ocho fallos: siete entradas aceptadas sin medición válida y el
reporte vacío por un período fijo que ya no contenía la fecha del sistema. Tras la corrección:

- Nueve pruebas del comando público aprobadas, incluida la ejecución en procesos distintos.
- `pnpm lint` y `pnpm typecheck` aprobados; `pnpm test`: **1.316 pruebas / 199 archivos**.
- `git diff --check`, JSON del manifiesto y 97 enlaces locales comprobados.
- Typecheck global y Vitest requirieron ejecución fuera del sandbox por `spawn EPERM`.
  La suite completa usó el OpenSSL instalado con Git, añadido al PATH del proceso.
- No se recompilaron artefactos: este corte cambia el instrumento fuera de `src/`, sus pruebas
  y la documentación, sin cambios en el código productivo.

Prueba de humo posterior a las suites, protocolo 2, crecimiento con 203 productos e historias
de 100/1.000/10.000 movimientos, 300 ventas, warm-up 1 y muestra 3:

```bash
pnpm --filter @supermarket/server perf -- --profile crecimiento --scenario report-sales --scenario report-inventory --warmup 1 --sample 3
```

- `report-sales`: mediana 1,264 ms; serie `.perf/2026-09-12T20-22-50-513Z`.
- `report-inventory`: mediana 9,033 ms; serie `.perf/2026-09-12T20-23-04-344Z`.

Ambas identifican `ee65cc3` con cambios pendientes y el SHA-256 del arnés
`6aedf4cd6fffe04a6e4c21d2abc20685478590164453a0cd134f242259fcd788`. Son verificaciones del
instrumento, no BEFORE ni presupuestos; no son comparables con el piloto del protocolo 1.
Los crudos permanecen ignorados y el resumen se conserva aquí.

La decisión pendiente es la carga de uso representativa: productos, cajas simultáneas,
ventas diarias, historia y hardware previsto. El plan conserva los siguientes cortes de
instrumentación; 12.01 permanece abierta y no habilita 12.02.

## Corte de jornada simulada — 2026-09-12

Segundo corte del [plan](./plan-12.01-baseline.md). El escenario 4 queda instrumentado entero:
`cash-shift-open` mide la apertura de caja —cada repetición estrena su caja, creada fuera de la
medición, porque una caja admite un solo turno abierto— y `sale-journey` agrega la emisión del
documento fiscal simulado desde la venta ya completada, con el contenido derivado por el nodo de
los snapshots que la venta congeló: la medición no vuelve a declarar importes ni impuestos.

Cada repetición exige el documento en `ISSUED`, modo `SIMULATION` y con número fiscal. Al cerrar
la serie, y fuera de toda medición, se verifica que el turno asentó exactamente lo cobrado y que
existe un `SALE_ISSUE` y una factura emitida por venta; el recuento se publica en
`environment.checks` de ambos artefactos. Un escenario que no dejó sus asientos aborta la serie.

- Once pruebas del comando público aprobadas, dos de ellas nuevas.
- `pnpm lint` y `pnpm typecheck` aprobados; `pnpm test`: **1.318 pruebas / 199 archivos**.
- Sin cambios en código productivo: el corte toca el arnés, sus pruebas y la documentación.

Prueba de humo posterior a las suites, protocolo 2, perfil habitual, warm-up 1 y muestra 3:

```bash
pnpm --filter @supermarket/server perf -- --scenario cash-shift-open --scenario sale-journey --warmup 1 --sample 3
```

- `cash-shift-open`: mediana 2,979 ms; serie `.perf/2026-09-12T20-35-35-726Z`; cinco turnos
  abiertos —cuatro repeticiones y el de la estación—.
- `sale-journey`: mediana 27,530 ms; serie `.perf/2026-09-12T20-35-37-947Z`; cuatro ventas, con
  sus cuatro facturas emitidas, ocho asientos de cobro y cuatro salidas de inventario.

Ambas identifican `29b7091` con cambios pendientes y el SHA-256 del arnés
`93617f3c0fd1d94b5766abc11bb1088c5cf4a81e86ea19a38c5246bc898f65f7`. Verifican el instrumento:
no son BEFORE ni presupuestos, y una muestra de 3 no fija ruido.

### La jornada gravada no se puede medir completa

Instrumentar la emisión reprodujo un defecto del producto, no del arnés: una venta que cobró
IGTF no puede emitir su factura —`400 FISCAL_TOTALS_INCONSISTENT`—, porque el total del
documento incluye el impuesto y la suma de sus líneas no, mientras `FiscalDocument` exige que
líneas, total y pagos sean el mismo entero. Queda registrado como **D-003** en
[defectos conocidos](../defectos-conocidos.md) con su reproducción y su alcance. Corregirlo exige
decidir cómo representa el IGTF un documento fiscal, que es una decisión normativa y está fuera
del alcance de 12.01.

Por eso el lote mixto de la jornada medida cobra con dos métodos **no** gravados —efectivo y
transferencia— y el dataset conserva sembrada la política de IGTF. Es un cambio de datos: abre
serie nueva y no se compara con las corridas anteriores.

## Corte de Electron e ingreso reales — 2026-09-14

Tercer corte del [plan](./plan-12.01-baseline.md). `login-and-shell` construye el renderer y el
proceso principal antes de medir, sirve `/app/` desde Fastify sobre un puerto loopback efímero y
lanza un Electron compilado por repetición. Cada proceso usa un perfil Chromium nuevo y oculto:
espera el formulario real, ingresa con la identidad sintética por HTTP, exige que aparezca el
shell autorizado y recarga la página para comprobar la recuperación de la sesión.

Las tres duraciones permanecen separadas: `desktop-to-login` nace antes de crear el proceso
Electron; `login-to-shell` comienza al enviar el formulario; `session-recovery` empieza con la
recarga de Chromium. El build y la semilla quedan fuera de las observaciones. El resumen conserva
los hashes del `main` y del HTML ejecutados. El perfil Chromium se elimina siempre al terminar,
por lo que la cookie no entra en `.perf/`; stderr se drena sin publicarlo porque Chromium puede
incluir rutas locales.

- Doce pruebas del comando público aprobadas, incluida la nueva ejecución Electron → HTTP →
  Fastify → SQLite → renderer.
- `pnpm lint` y `pnpm typecheck` aprobaron. `pnpm test` aprobó **1.319 pruebas / 199 archivos** al
  incorporar `C:\Program Files\Git\usr\bin` al `PATH`; la primera ejecución, sin esa ruta, dejó
  únicamente las dos fallas esperadas de `OPENSSL_NOT_AVAILABLE` y no reveló otra regresión.
- El build de desktop, el test E2E focal y la suite completa corrieron fuera del sandbox porque
  Vitest, esbuild y Electron necesitan crear procesos hijos en Windows.

Prueba de humo posterior a las suites, protocolo 2, perfil habitual, warm-up 1 y muestra 3:

```bash
pnpm --filter @supermarket/server perf -- --scenario login-and-shell --warmup 1 --sample 3
```

- `desktop-to-login`: mediana 384,137 ms.
- `login-to-shell`: mediana 53,643 ms.
- `session-recovery`: mediana 43,724 ms.

La serie `.perf/2026-09-15T03-34-08-405Z` —nombre UTC; corrida local del 2026-09-14— identifica
`384bbb9` con cambios pendientes, hash compuesto del arnés
`393260d4ae51a86d92189fff9b812d5477042e92bda113d27781e565469db69c`, `electronMain`
`23c30594f68fd8c48babf8c66086c00f93d8808c711dfbc84c31fc8817dc5dd8` y `rendererHtml`
`5a0cdcd8a84726ec2ca8565d9a92f3bbe80575a7e7e4d9fd13e20c36c56b78f9`. Es verificación del
instrumento: tres observaciones no fijan ruido, BEFORE ni presupuestos. El nodo ya estaba listo
antes de cada Electron; primera instalación y reinicio con base existente permanecen separados y
pendientes dentro del escenario 1. El corte no habilita 12.02.
